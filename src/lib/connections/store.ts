import "server-only";
import { createSupabaseAdminClient, createSupabaseServerClient } from "@/lib/supabase/server";
import { encryptCredential } from "@/lib/security/crypto";
import type { ProviderName } from "@/lib/providers/contracts";

export async function assertWorkspaceOwner(workspaceId:string){const supabase=await createSupabaseServerClient();const admin=createSupabaseAdminClient();if(!supabase||!admin)throw new Error("Seed's credential store is not configured.");const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Authentication required.");const{data:profile}=await admin.from("profiles").select("id").eq("auth_user_id",user.id).maybeSingle();if(!profile)throw new Error("Seed profile not found.");const{data:workspace}=await admin.from("workspaces").select("id").eq("id",workspaceId).eq("owner_user_id",profile.id).maybeSingle();if(!workspace)throw new Error("Workspace access denied.");return{admin,user,profile};}
export async function storeProviderConnection(workspaceId:string,provider:ProviderName,accessData:Record<string,unknown>,scopes:string[]=[]){const{admin,profile}=await assertWorkspaceOwner(workspaceId);const encrypted_access_data=encryptCredential(JSON.stringify(accessData));const{error}=await admin.from("provider_connections").upsert({workspace_id:workspaceId,provider,encrypted_access_data,scopes,status:"connected",connected_at:new Date().toISOString(),updated_at:new Date().toISOString()},{onConflict:"workspace_id,provider"});if(error)throw new Error("Seed could not save the provider connection.");await admin.from("audit_events").insert({actor_user_id:profile.id,workspace_id:workspaceId,tool_name:`connect_${provider}`,metadata:{provider,status:"connected"}});}
